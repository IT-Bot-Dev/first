import { initDayMode } from './night-mode.js';
import { initLocalization } from './localization.js';


document.addEventListener('DOMContentLoaded', () => {

    initDayMode();

    initLocalization();

});
